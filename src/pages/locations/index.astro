---
import DashboardLayout from '../../layouts/DashboardLayout.astro';
import LocationTableIsland from '../../components/locations/LocationTableIsland';
---

<DashboardLayout title="Locations & Offices">
    <div class="h-full flex flex-col">
        <!-- Header -->
        <header class="flex-none px-6 py-6 border-b border-gray-200 dark:border-gray-700 bg-white dark:bg-surface-elevated">
            <div class="flex items-center justify-between max-w-7xl mx-auto">
                <div>
                    <h1 class="text-2xl md:text-3xl font-bold text-gray-900 dark:text-white tracking-tight leading-tight">
                        Locations & Offices
                    </h1>
                    <p class="mt-2 text-sm text-gray-600 dark:text-gray-400">
                        Manage physical offices, warehouses, and remote zones where assets are stored or assigned.
                    </p>
                </div>
            </div>
        </header>

        <!-- Main Content (Island) -->
        <main class="flex-1 overflow-y-auto bg-gray-50 dark:bg-surface-base p-6">
            <div class="max-w-7xl mx-auto">
                <LocationTableIsland client:load />
            </div>
        </main>
    </div>
</DashboardLayout>
